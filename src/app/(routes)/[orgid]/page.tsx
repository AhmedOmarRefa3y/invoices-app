import NewHomePAge from "@/components/NewHomePAge";

const page = async () => {
    return (
        <div className="h-full mx-auto w-full">
            <NewHomePAge />
        </div>
    );
};

export default page;
